/**
 * Prefixes for storing files when the only differintiator is a record id
 */
export type FilePrefix = 'userprofile' | 'organizationlogo';

/**
 * Files sizes in different conversions
 */
export enum FileSizes {
  MB = 1000000, // bytes
}
